dane: pesel + adres (do ubezpieczenia), terminy przyjazdu (default - cale warsztaty), preferowany rozmiar koszulki, uwagi dodatkowe (np. wegetarianin)
zapłacono
show profile: CV + warsztaty, punkty

tinymce <code> (task solution - at least tabs)
checklista ("dodaj zadania kwalifikacyjne", "sprawdź rozwiązania od N uczestników", ...)
sprawdzać łączny czas trwania warsztatów 9*2*2+2 (+info, krótsze pobyty)?
==Zadania kwalifikacyjne==
add to user todo checklists
green checkmark on list
email notifications
feedback latex?
zmiana zadań kwalifikacyjnych, warning?
add some logs and log filtering


--bugs
ie6 fixed height?!?

---Technical
.htaccess
separate form handling functions
cleanup cleanup cleanup
parseUserHTML
database sql aaaargh
remove the meaningless error strings in every db_query
autoversion .css, .js
fix <tr>-less <thead>s
change templates to 'endif' syntax
co to jest id="theform"?
when creating emails, change $email to $content or something, because it's misleading
check perms in task solution forms
merge participant-lecturer columns

$DB
	$DB['tablename']['primarykey']['field'] access
	default last resource
	enum
forms:
	if !empty POST and function_exists s/action/form/g
