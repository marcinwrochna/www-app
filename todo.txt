Top priority:
check update30: insertPermission(admin-impersonate, registered-editTutoringApplication)

nieznana akcja: 500.shtml
coś z /tinymce
events - make the app support WWW7 as soon as possible
restore tutoring
Signup/Resign button rights are inconsistent.
There's a sick amount of w1_users_login_key indexes in local pgdb
actualize sitemap and others

Zlecone:
- sortowanie po nazwisku (lub chociaż po imieniu) tabelek wypluwanych przez aplikację
- zliczanie rozmiarów koszulek
- eksport danych w jakimś sensownym formacie (może da się podpiąć pod Googledocs API?)

Zlecalne:
do parseUserHTML() dodać HTMLPurifier'a
common.js: tipon() - wyczyścić i sprawdzić y
loguj czy i kto impersonuje, jeśli ustawiono $USER['impersonatedBy'] (log.php::logUser(), dodatkowa kolumna do DB i wyświetlanie w logu)
podczas impersonowania maile wyświetlaj zamiast wysyłać
dodać do DB->users kolumnę "surname", uaktualnianą przy zmianie "name",
	która by ułatwiała i przyspieszała sortowanie po nazwisku (obecnie regexp_replace(u.name,\'.*\ ([^\ ]+)\',\'\\\\1\'))

zapłacono
show profile: CV + warsztaty, punkty

tinymce <code> (task solution - at least indentation)
checklista ("dodaj zadania kwalifikacyjne", "sprawdź rozwiązania od N uczestników", ...)
sprawdzać łączny czas trwania warsztatów 9*2*2+2 (+info, krótsze pobyty)?
==Zadania kwalifikacyjne==
add to user todo checklists
email notifications
feedback latex?
zmiana zadań kwalifikacyjnych, warning?
add some logs and log filtering
add role instead of lack of 'uczestnik' role (e.g. rights to autoQualifyForWorkshop).
ustalić normalizację punktów (jest 0..20, 0..6, 0..400. Proponuję więc 0..100 :P)
'nocleg i wyżywienie we własnym zakresie' kind of makes no sense in profile,
	maybe 'zamierzam = poprowadzić warsztaty | robić zadania kwalifikacyjne | przychodzić z Olsztyna'

--bugs
ie6 fixed height?!? // May have been fixed after google IE fix scripts change. If not, try css:zoom:1 (compare szykujpieluchy.pl)
see why TinyMCE includes html-escaped comment tags with MS Word stuff.

---Technical
---- Ongoing (mostly done, except for tasks.php and tutoring)
worshops.php: cleanup the js field, change handleEditWorkshopForm to actionEditWorkshopForm (unless $form->getValues())
.htaccess - href="?action=abc&amp;param=x" should be href="abc(x)". Test IE6.
transfer lists of constants to enum.php
change db_() to $DB->
change showMessage() to $PAGE->addMessage()
change <form> to new Form()
move lecturer db-column to participant=enumParticiapntStatus('lecturer')
checkUserCanEditTasks() seems stupid
the whole app is one big XSS hole.

change templates to '<?php if (): ?>\n something \n<?php endif;?>' syntax where applicable.
change to $class = alternate('even','odd');
-- Cleanup after the big changes
ALTER TABLE table_workshop_user DROP COLUMN lecturer;
$DB->options['domains']->delete();
grep -i deprecated 
remove form.php backwards compatibility

--- Waiting
fix <tr>-less <thead>s (they're not w3c-compliant, I thought the syntax was <table><thead><th></th></thead><tr><td></td></tr></table>, it's not)
co to jest id="theform"?
when creating emails, change $email to either $content or $address or something, because it's misleading
check perms in task solution forms
make email X-Sender tag (or lack of it) explicit in config.php:sendMail
pg_prepare some statements?
escape some fields (actually any user can set his school to an XSS attack-script), test with (at least) 'Abćđœ\\n+Plus%25 '. "\n" .'"DQuoted" O\'Brian &&amp; <Company>';
change roles to sth more natural
change to only one subject?
Split showWorkshop into tabs (propsition, zapisani [i|,] kwalifikacja, edit), split warsztaty.php,tasks.php

-- Ideas
if(Form::submitted() and function_exists($action .'Form'))?
school/univ autocomplete (zlecalne?)
add lecturers when creating workshop
buildMenuBox perm=true redundant?
give kadra possibility to impersonate Tester (zlecalne?)
better SELECT, DB_Slice?
callAction, automatically log action(param). auto check permissions?
make the app more open - publish lists, user profiles, ...
